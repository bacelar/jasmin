modsignature { param u64 ab; }


require "file2.jazz"
from ABCd require "../common/file1.jazz" as XYZ with { ab = abXXX; }

export fn main(reg u32 x y) -> reg u32 {
   reg u32 r1 r2;
   r1 = add(x,y);
   r2 = r1;
   return r2;
}

